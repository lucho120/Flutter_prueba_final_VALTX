import 'package:flutter/material.dart';

class DetailFoodView extends StatelessWidget {
  const DetailFoodView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DetailFoodView'),
      ),
    );
  }
}
